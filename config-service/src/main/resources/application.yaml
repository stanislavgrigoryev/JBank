spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/igorkosandyak/demo-config-server
          clone-on-start: true
      fail-fast: true
      enabled: false
server:
  port: 9297

#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka/
#  instance:
#    prefer-ip-address: true
